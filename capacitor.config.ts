import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.varunpublicschoolss.app',
  appName: 'Varun Public School',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
